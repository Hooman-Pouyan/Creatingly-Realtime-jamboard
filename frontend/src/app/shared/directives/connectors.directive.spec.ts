import { ConnectorsDirective } from './connectors.directive';

describe('ConnectorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectorsDirective();
    expect(directive).toBeTruthy();
  });
});
