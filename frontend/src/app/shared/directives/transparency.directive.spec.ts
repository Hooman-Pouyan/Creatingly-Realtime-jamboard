import { TransparencyDirective } from './transparency.directive';

describe('TransparencyDirective', () => {
  it('should create an instance', () => {
    const directive = new TransparencyDirective();
    expect(directive).toBeTruthy();
  });
});
