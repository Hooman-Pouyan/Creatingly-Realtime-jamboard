import { AppearenceDirective } from './appearence.directive';

describe('AppearenceDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearenceDirective();
    expect(directive).toBeTruthy();
  });
});
