import { PinDirective } from './pin.directive';

describe('PinDirective', () => {
  it('should create an instance', () => {
    const directive = new PinDirective();
    expect(directive).toBeTruthy();
  });
});
