import { UtilityDirective } from './utility.directive';

describe('UtilityDirective', () => {
  it('should create an instance', () => {
    const directive = new UtilityDirective();
    expect(directive).toBeTruthy();
  });
});
