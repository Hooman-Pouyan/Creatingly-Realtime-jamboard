import { EventtypePipe } from './ConvertToProperty.pipe';

describe('EventtypePipe', () => {
  it('create an instance', () => {
    const pipe = new EventtypePipe();
    expect(pipe).toBeTruthy();
  });
});
